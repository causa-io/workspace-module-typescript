version: 1

workspace:
  name: causa

project:
  name: workspace-module-typescript
  description: The Causa workspace module providing functionalities for projects coded in TypeScript.
  language: typescript
  type: package

causa:
  modules:
    '@causa/workspace-core': '>= 0.15.0'
    '@causa/workspace-typescript': '>= 0.4.0'

javascript:
  dependencies:
    update:
      packageTargets:
        '@types/node': minor
